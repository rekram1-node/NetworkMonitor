name: CI/CD

on: [push, pull_request]
# on:
#   push:
#     branches:
#       - main

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.19.x
      
      - name: Bump version and push tag
        uses: anothrNick/github-tag-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WITH_V: true

      - name: Docker Login
        uses: docker/login-action@v1
        with:
          registry: docker.io
          username: rekram
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v2
        with:
          version: latest
          args: release --rm-dist
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          DOCKER_TOKEN: ${{ secrets.DOCKER_ACCESS_TOKEN }} 
      
      - name: Publish to all 
        run: |
          docker images
          echo ${{ secrets.DOCKER_ACCESS_TOKEN }} | docker login --username rekram --password-stdin
          docker push rekram/network-monitor


      
      # - name: Log in to the Container registry
        
      #   echo ${{ secrets.GH_PAT }} | docker login $CI_REGISTRY --username $CI_REGISTRY_USER --password-stdin

